apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: recommend-secret
spec:
  dataFrom:
    - find:
        path: /moyeobab/recommend/dev/
        name:
          regexp: ".*"
      rewrite:
        - regexp:
            source: ".*/(.*)"
            target: "$1"
