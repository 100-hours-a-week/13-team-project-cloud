apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: chatbot-secret
spec:
  dataFrom:
    - find:
        path: /moyeobab/chatbot/dev/
        name:
          regexp: ".*"
      rewrite:
        - regexp:
            source: ".*/(.*)"
            target: "$1"
