apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: recommend-secret
spec:
  dataFrom:
    - find:
        conversionStrategy: Default
        decodingStrategy: None
        path: /moyeobab/recommend/prod/
        name:
          regexp: ".*"
      rewrite:
        - regexp:
            source: ".*/(.*)"
            target: "$1"
