apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: backend-secret
spec:
  dataFrom:
    - find:
        conversionStrategy: Default
        decodingStrategy: None
        path: /moyeobab/spring/prod/
        name:
          regexp: ".*"
      rewrite:
        - regexp:
            source: ".*/(.*)"
            target: "$1"
