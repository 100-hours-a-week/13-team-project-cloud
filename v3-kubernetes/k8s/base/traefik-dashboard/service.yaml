apiVersion: v1
kind: Service
metadata:
  name: traefik-dashboard
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "traefik-dashboard"
spec:
  selector:
    app.kubernetes.io/name: traefik
  ports:
    - port: 9000
      targetPort: 9000
