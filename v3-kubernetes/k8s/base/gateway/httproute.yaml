apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: moyeobab-routes
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  rules:
    # /api/v2/recommend → recommend 서비스
    - matches:
        - path:
            type: PathPrefix
            value: /api/v2
      backendRefs:
        - name: recommend
          port: 8000
    # 나머지 → backend 서비스
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: backend
          port: 8080
