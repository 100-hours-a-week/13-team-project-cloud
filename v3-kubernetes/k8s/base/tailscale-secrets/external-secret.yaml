apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: operator-oauth
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: aws-ssm
  target:
    name: operator-oauth
  dataFrom:
    - find:
        path: MUST_BE_OVERRIDDEN_BY_OVERLAY
        name:
          regexp: ".*"
      rewrite:
        - regexp:
            source: ".*/(.*)"
            target: "$1"
